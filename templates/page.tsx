import type { NextPage } from "next";
import Head from "next/head";

const PAGE_NAME: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Wagmi | frens</title>
        <meta name="description" content="To the moon" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main></main>
    </div>
  );
};

export default PAGE_NAME;
